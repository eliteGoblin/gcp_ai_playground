# =============================================================================
# Custom Code Dependencies
# =============================================================================
# These are installed in the serving container alongside your model

# Model loading
transformers>=4.36.0
torch>=2.0.0
accelerate>=0.25.0
safetensors>=0.4.0

# Tokenization
sentencepiece>=0.1.99
protobuf>=3.20.0

# Utilities
huggingface_hub>=0.19.0

# Note: The base container (pytorch-vllm-serve) already has most of these.
# Only add packages that are MISSING from the base container.
